# This program stores names in a list and prints them individually

names = ["Lewis", "Lylod", "Luigi"] # Square brackets creates an array

print(names[0]) # 0 is the first index in the array; 1 and 2 are the consecutive indices in the array
print(names[1])
print(names[2])